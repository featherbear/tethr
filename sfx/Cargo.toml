[package]
name = "tethr-sfx"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "sfx"
path = "src/main.rs"

[dependencies]
zip = { version = "2", default-features = false, features = ["deflate"] }

[profile.release]
strip = true
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
